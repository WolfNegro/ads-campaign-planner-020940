flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
sqlalchemy==2.0.36
marshmallow==3.22.0
weasyprint==62.3
pandas==2.2.3
xlsxwriter==3.2.0
gunicorn==22.0.0
# Dev
black==24.10.0
isort==5.13.2
flake8==7.1.1
pytest==8.3.3
pytest-cov==5.0.0
pre-commit==3.8.0
SQLAlchemy==2.0.36
"EOF"
pip install -r requirements.txt

# app base
cat > app/__init__.py <<EOF
from flask import Flask
from dotenv import load_dotenv
import os

def create_app():
    load_dotenv()
    app = Flask(__name__, static_folder="static", template_folder="templates")
    app.config["SECRET_KEY"] = os.getenv("SECRET_KEY", "dev")
    app.config["JSON_SORT_KEYS"] = False

    from .routes.main import bp as main_bp
    app.register_blueprint(main_bp)

    @app.errorhandler(404)
    def not_found(_):
        return {"error": "Not found"}, 404

    return app
"EOF"

cat > app/routes/__init__.py <<EOF
# Blueprints namespace
"EOF"

cat > app/routes/main.py <<EOF
from flask import Blueprint, jsonify, render_template

bp = Blueprint("main", __name__)

@bp.get("/health")
def health():
    return jsonify(status="ok")

@bp.get("/")
def home():
    return render_template("index.html", title="Planificador de Campañas")
"EOF"

# templates + assets
cat > app/templates/base.html <<EOF
<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title or "Planificador" }}</title>
    <link rel="stylesheet" href="{{ url_for("static", filename="css/app.css") }}">
  </head>
  <body>
    <header class="container">
      <h1>Planificador de Campañas</h1>
    </header>
    <main class="container">
      {% block content %}{% endblock %}
    </main>
    <script src="{{ url_for("static", filename="js/app.js") }}"></script>
  </body>
</html>
"EOF"

cat > app/templates/index.html <<EOF
{% extends "base.html" %}
{% block content %}
  <section class="card">
    <h2>¡Bienvenido!</h2>
    <p>Base lista para construir estrategia, estructura y modo presentación.</p>
    <ul>
      <li>Healthcheck: <code>/health</code></li>
    </ul>
  </section>
{% endblock %}
"EOF"

cat > app/static/css/app.css <<EOF
:root { --bg:#0b0c10; --card:#121318; --text:#e8e8e8; --muted:#9aa0a6; --accent:#4f46e5; }
*{box-sizing:border-box} body{margin:0;background:var(--bg);color:var(--text);font:16px/1.45 system-ui,-apple-system,Segoe UI,Roboto,Ubuntu}
.container{max-width:960px;margin:32px auto;padding:0 16px}
h1,h2{margin:0 0 12px} h1{font-size:28px} h2{font-size:22px}
.card{background:var(--card);border:1px solid #1e1f27;border-radius:16px;padding:20px;box-shadow:0 10px 30px rgba(0,0,0,.25)}
a{color:var(--accent);text-decoration:none}
code{background:#1b1d24;padding:.2em .4em;border-radius:6px}
"EOF"

cat > app/static/js/app.js <<EOF
console.log("UI base cargada");
"EOF"

# runner
cat > run.py <<EOF
from app import create_app
app = create_app()
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000, debug=True)
"EOF"

# VS Code config mínima
mkdir -p .vscode
cat > .vscode/settings.json <<EOF
{
  "python.defaultInterpreterPath": ".venv/bin/python",
  "editor.formatOnSave": true,
  "python.analysis.typeCheckingMode": "basic",
  "editor.codeActionsOnSave": { "source.organizeImports": true },
  "[python]": { "editor.defaultFormatter": "ms-python.black-formatter" },
  "flake8.args": ["--max-line-length=100"]
}
"EOF"

# git básico
cat > .gitignore <<EOF
.venv/
__pycache__/
*.pyc
.env
.weasyprint/
"EOF"
git init -q

# abrir en VS Code (WSL)
code .

echo
echo "✅ Proyecto creado en: /home/wsl/ads-campaign-planner-020940"
echo "▶ Arranca así: source .venv/bin/activate && python run.py  (luego abre http://localhost:5000)"
